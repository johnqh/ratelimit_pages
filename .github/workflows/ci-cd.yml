---
# CI/CD workflow for ratelimit_pages
# Publishes to npm on main branch

name: CI

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop

permissions:
  contents: write
  id-token: write
  deployments: write

jobs:
  ci:
    uses: johnqh/workflows/.github/workflows/unified-cicd.yml@main
    secrets: inherit
